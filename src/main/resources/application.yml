logging:
  file: handicapper.log
  
spring:
  datasource:
    password: ******
    url: jdbc:mysql://localhost:3306/topics
    username: root
   
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    database: mysql
